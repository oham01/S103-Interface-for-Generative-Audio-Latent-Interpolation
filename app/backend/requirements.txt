# Dependencies from SCAPES
torchdiffeq
torch
torchaudio
librosa
numpy
matplotlib
soundfile
transformers
tqdm
msclap

# Backend dependencies
fastapi
uvicorn
pydantic
scikit-learn
# SCAPES's FlowInference.py imports IPython.display at module load time
# (notebook-era artifact). Install it so the import succeeds in a server context.
ipython